import { Typography } from '@mui/material';
import { CustomNode } from '@mern-monorepo/ui-react-template';

export default function JobNode() {
  return (
    <CustomNode>
      <Typography variant="h5"> Endpoint</Typography>
    </CustomNode>
  );
}
